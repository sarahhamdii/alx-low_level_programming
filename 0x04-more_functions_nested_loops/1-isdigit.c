#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 * @c: character
 * Return: 1 for digit or 0 for anything else
 */

int _isdigit(int c)
{
	if (c >= '0'; c <= '9')
	{
		return (1);
	}
	return (0);
}
